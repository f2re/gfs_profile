# Маршрутный вертикальный профиль GFS

## Назначение

`/route` строит модельный вертикальный разрез атмосферы вдоль маршрута. Для каждой расчётной точки координата и срок прогноза определяются из длины маршрута, заданной средней скорости и заблаговременности вылета.

Продукт использует GFS 0.25° и не является авиационным разрешением, SIGMET, GAMET, METAR, TAF или радиозондом.

Строгие требования к визуальной иерархии, цветам, паттернам, карточкам и ограничениям Telegram зафиксированы в [`ROUTE_PROFILE_VISUAL_REQUIREMENTS.md`](ROUTE_PROFILE_VISUAL_REQUIREMENTS.md).

## Использование

Пошаговый режим:

```text
/route
Москва -> Санкт-Петербург
```

Экспертная команда:

```text
/route Москва -> Санкт-Петербург +24 speed=300 mode=pro
/route 55.75 37.62 -> 59.94 30.31 +6 speed=450 mode=simple
```

Параметры:

- `+24` или `lead=24` — заблаговременность вылета от run модели;
- `speed=300` — средняя путевая скорость, 50–1000 км/ч;
- `mode=simple|pro` — простой или профессиональный режим;
- `run=YYYYMMDD/HH` — фиксированный цикл GFS для воспроизводимого расчёта.

## Единицы измерения

Маршрутный продукт использует только метрическую систему:

- путевая скорость — км/ч;
- скорость атмосферного ветра — м/с;
- расстояние — км;
- высота — м или км;
- давление — гПа;
- температура — °C;
- осадки — мм;
- видимость — км.

Узлы и футы в `/route` не используются.

## Расчёт маршрута

- расстояние и промежуточные точки вычисляются по дуге большого круга;
- обычный шаг — один час движения;
- для очень длинного маршрута шаг увеличивается так, чтобы получить не более 25 точек;
- срок каждой точки равен `lead вылета + ETA`; он привязывается к допустимому сроку GFS;
- для всех точек используется один опубликованный цикл GFS, содержащий срок прибытия;
- вертикальный профиль ограничен уровнями 1000…500 гПа.

## Единые обозначения

Температура используется как приглушённый фон и не должна перекрывать опасные явления.

- синий точечный слой и `❄ ЛЁД` — диагностический риск обледенения;
- оранжевая диагональная штриховка и `≈ БОЛТ.` — вертикальный сдвиг ветра / болтанка;
- тёмно-синие полосы и контуры — сильный ветер;
- фиолетовый `⚡` — модельная конвекция или гроза;
- синий `●` — осадки без выделенной грозы;
- нейтральная серо-синяя заливка — облачный слой.

Цвет всегда дублируется паттерном или символом. Легенда располагается вне рабочей области разреза.

## Простой режим

`simple` предназначен для быстрого чтения без знания метеорологических изолиний.

Он показывает:

- приглушённый температурный фон;
- крупные подписи зон обледенения и болтанки;
- выделенные зоны сильного ветра со скоростью в м/с;
- значки грозы и осадков;
- карточки участков маршрута с диапазоном километров, сроком, UTC-временем и 1–3 приоритетными символами.

Статусы карточек:

- `СПОКОЙНО`;
- `ВНИМАНИЕ`;
- `СЛОЖНО`;
- `ВЫСОКИЙ РИСК`.

Это модельная классификация, а не запрет или разрешение на полёт.

## Профессиональный режим

`pro` сохраняет те же визуальные обозначения рисков и добавляет только ключевые диагностические слои:

- ветровые барбы с разрежением по точкам и уровням;
- контуры скорости ветра `V20/V30/V40`, м/с;
- изотермы `0/-10/-20 °C`;
- контуры RH `80/90%`;
- в карточках: `Vmax`, осадки, минимальная видимость и ВНГО;
- CSV по каждой точке и уровню.

Числовые подписи ограничены: в карточке не более двух строк показателей, изолинии подписываются разреженно.

## Карточки участков

Карточки соответствуют участкам между соседними точками. Если участков больше 12, соседние участки агрегируются в не более чем 12 карточек.

Для агрегированной карточки используются:

- максимальный риск;
- максимум ветра и осадков;
- минимум видимости и ВНГО;
- срок и UTC-время начала участка;
- до четырёх приоритетных символов в `pro` и до трёх в `simple`.

## Ограничения

Диагностика обледенения и болтанки является эвристикой по модельным T/RH и вертикальному сдвигу ветра. Она не заменяет специализированные авиационные продукты. Перед реальным полётом обязательны актуальные METAR/TAF/SIGMET/GAMET, NOTAM, данные диспетчерских и решение командира воздушного судна.
